# Django and core dependencies
Django>=5.2.3,<6.0
django-extensions>=3.2.0

# Database
# djongo>=1.3.6          # Django MongoDB connector (has conflicts with Django 5.x)
pymongo>=4.0.0         # MongoDB driver
dnspython>=2.6.0       # Required for MongoDB connection strings
mongoengine>=0.29.0    # Modern MongoDB ODM for Django

# API Framework
djangorestframework>=3.15.0
django-filter>=23.0.0
django-cors-headers>=4.0.0

# Authentication
django-allauth>=0.63.0     # Social authentication (Google, GitHub, etc.)

# Caching and background tasks
redis>=5.0.0
celery>=5.3.0
django-celery-beat>=2.5.0

# Text processing and utilities
markdown>=3.5.0
bleach>=6.0.0
python-slugify>=8.0.0

# HTTP requests (for external APIs)
requests>=2.31.0
httpx>=0.25.0

# Image processing (for card images)
Pillow>=10.0.0

# Development tools
django-debug-toolbar>=4.2.0
ipython>=8.15.0

# Production server
gunicorn>=21.0.0
whitenoise>=6.5.0

# Testing
pytest>=7.4.0
pytest-django>=4.6.0
factory-boy>=3.3.0

# Environment management
python-decouple>=3.8
python-dotenv>=1.0.0

# Data import/export
openpyxl>=3.1.0
pandas>=2.1.0

# Monitoring (optional)
sentry-sdk>=1.32.0
